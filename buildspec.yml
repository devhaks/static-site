version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    commands:
      - pwd
      - ls -al
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      # - command
      # - command
  #post_build:
    #commands:
      # - command
      # - command
artifacts:
  files:
    - index.html
    - static/*
  name: dist
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
